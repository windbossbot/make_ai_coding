services:
  - type: web
    name: make-ai-coding
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.web_service:app --host 0.0.0.0 --port $PORT
