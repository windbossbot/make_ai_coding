services:
  - type: web
    name: make-ai-coding
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run scripts/web_app.py --server.address 0.0.0.0 --server.port $PORT
